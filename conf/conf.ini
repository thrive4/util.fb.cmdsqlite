[general]
mapversion   = 1.0

[application]
' options: verbose, full
logtype         = verbose
' used for debug build
usecons         = false
' options: en, de, fr and nl
locale          = en

[output]
' deprecated use optional fourth parameter extra
' additional parsing per filetype mp3, jpg
' options: default, extra
' htmloutput      = extra
